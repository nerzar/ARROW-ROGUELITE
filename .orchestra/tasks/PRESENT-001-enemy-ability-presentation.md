# TASK: PRESENT-001 — Презентация способностей врагов: переход между сторонами, stagger, heal, лут, щит

STATUS: DONE
TYPE: BUILD (presentation only)
SIZE: M
AGENT: (подходит для Gemini / AI Studio — presentation-слой, движок не трогать)
BASE_BRANCH: main
BRANCH: build/PRESENT-001-ability-presentation

## Цель

Механики уже в движке и видны только текстом в HUD/логе. Игрок должен **видеть** событие раньше,
чем прочтёт его.

## Контекст

Файлы: `spikes/arrow-core/viewer/visual-proto/board-renderer.js` (арена/враги/HUD),
`enemy-visual-state.js` (состояния и тайминги поз), `hit-fx.js` (VFX-003), `projectile-flight.js`.
События приходят в `TapResult` из `EncounterState` (`src/encounter.ts`): `shifted[{id,from,to}]`,
`healed[{id,target,amount}]`, `rewards[{id,heal,rotate}]`, `expired[{id}]`, `pinnedThisTurn[]`,
`shieldRaised[]`, `shieldConsumed[]`. Стороны: 0=N (верх), 1=E (право), 3=W (лево).
Правило VFX (PROJECT.md): presentation only — не импортировать/не менять `EncounterState`, не трогать `src/`.

## Нужно

1. **Переход между сторонами** (`shifted`; разведчик — по таймеру, пьяница — по удару): спрайт за
   ~450 мс пробегает от старого подиума к новому по дуге за доской (образец — `fleeOx/fleeOy` у
   Goblin King), разворот по направлению, squash на старте, приземление с recoil; HUD-плашка едет
   вместе. Для пьяницы перед бегом — «stagger»: шаг назад, поза `hit`, звёздочки.
2. **Heal** (`healed`): от матроны к цели летит зелёно-золотая искра, у цели всплывает `+1`
   (тот же damage-number popup, зелёный), матрона на 300 мс в позе `attack`.
3. **Лут-цель** (`LEAVES IN N`, `rewards`, `expired`): пульс плашки при N ≤ 1; при убийстве —
   вспышка и `+2 HP` зелёным над игроком; при уходе — цель убегает за край (как flee).
4. **Щит** (`shieldRaised` / `shieldConsumed`): полупрозрачный бронзовый диск перед спрайтом;
   при блоке — треск, диск гаснет.
5. **Пин камнем** (`pinnedThisTurn`): проверить существующий визуал пина на плотных досках 7x7/8x8 —
   камень должен читаться на короткой стреле.

## Готово, если

- сцены «Разведчик» (4), «Пьяный дозор» (5), «Матрона» (8), «Капитан» (10), «Обоз» (11) на
  `http://localhost:5177/viewer/visual-proto/index.html` показывают события без чтения лога;
- `src/` не изменён, тесты зелёные, на 8x8 нет просадки кадров.

После сдачи STOP.

## RESULT

Работа предыдущего агента (Gemini) была практически полной — все 5 пунктов уже реализованы в
`board-renderer.js`/`app.js`: shift (stagger+arc run, обе разновидности триггера), heal (искра +
зелёный попап), щит (бронзовый диск, treshina на блок), лут (`lootLeaving`/`expired` ветки), пин —
не тронут. Я не переписывал готовую работу, только доделал и проверил.

Найден и исправлен один реальный баг: строка `УХОДИТ: N` добавлялась в массив `lines`, который
используется только для расчёта размера плашки (`hudBoxes`), но реальная отрисовка идёт через
отдельную систему "Unified Combat Frame" (см. блок с `cardW/cardH` в `drawTarget`), которая эту
строку никогда не рисовала — от `lootLeaving` был виден только пульсирующий золотой бордер, без
текста. Добавлена отрисовка `УХОДИТ: ${t.turnsLeft}` в левой части карточки (там же, где чипы
countdown/ability, которых у лут-целей не бывает). Заодно убрал хардкод `'1'`, теперь берёт
реальное `t.turnsLeft`.

## CHANGED FILES

- `spikes/arrow-core/viewer/visual-proto/board-renderer.js`
- `spikes/arrow-core/viewer/visual-proto/app.js`

`src/` не тронут.

## SCENES CHECKED

Через `window.visualDebug` (`tap`, `debugTargets`, `fx`, `layout`) + скриншоты в браузере:

- 9 · Гоблин-разведчик — shift по таймеру (from→to корректны, HUD едет вместе).
- 10 · Пьяный дозор — shift по удару (stagger → бег), `hitTriggered` синхронизирован с `FLIGHT_MS`.
- 13 · Матрона — heal: искра от кастера до цели (`healSpark.from` — реальный якорь), `+1` зелёным.
- 15 · Капитан стражи — щит: появление (`shieldUpT`), блок/треск (`shieldT`) на попадании.
- 16 · Обоз — предупреждение `УХОДИТ: N` (было сломано, исправлено), убийство с наградой
  (`deathT`/`lootText` стамп), истечение времени (`expired`→`fled`, текст «уходит!»/«ушёл!», уход
  за край, лог `GONE: ...`).
- 8 · Камнемёт (7x7) — пин камнем: камень сидит над остриём стрелы, не перекрывает наконечник.

## VERIFY

- `npm test` — 392/392 зелёных, после правки тоже (перезапускал).
- `npm run build` (`tsc`) — чисто.
- `git diff` — только два файла в `viewer/visual-proto/`, `src/` не изменён.
- Визуально: все сцены из чек-листа показывают события без чтения лога/HUD-текста.

## FOUND

- Лут-предупреждение `УХОДИТ: N` было не подключено к реальной отрисовке (см. RESULT) — исправлено
  в этой же ветке.
- Heal/loot-реворды не эмитят событие, если цель/игрок уже на максимуме (полный HP) — это похоже на
  осознанное поведение движка (нет смысла показывать +0), а не баг презентации; не трогал.
- Тулбар дев-вьюера перекрывает самый верх канваса на N-стороне при тесных сценах (`plate.y≈7px`) —
  предсуществующий эффект FIX-033-клампа, не специфичен для этой задачи, в чистом/боевом виде
  (без dev-toolbar) не проявляется.

## SHA

См. финальный commit на `build/PRESENT-001-ability-presentation`.
