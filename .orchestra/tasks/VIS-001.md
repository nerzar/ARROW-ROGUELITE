# TASK: VIS-001 — Playable Visual Combat Shell

STATUS: DONE
TYPE: BUILD
SIZE: L
AGENT: Claude (Sonnet 5)
BASE_BRANCH: main
BRANCH: build/VIS-001-combat-shell
START_SHA: c972cd1bea81429cb0b4b57019cb8d03380bc26d
RESULT_SHA: <filled after this file's own commit, see report below>
CODE_SHA: d25bb3b

## Что нужно сделать

Превратить debug-viewer (`cp-prologue.js`) в первый красивый 16:9 desktop visual-прототип
боевого экрана, без изменения игровой механики: реальные arrows из Level/BoardTopology
(не растровое изображение доски), premium-looking path rendering, layout под multi-enemy/boss,
presentation-only animation hooks, AssetManifest с graceful placeholder.

Полный исходный бриф пользователя сохранён в истории сессии/PR description, не дублируется
здесь дословно.

## Что важно знать

- `EncounterState`/`RunState` (spikes/arrow-core/src) — источник истины; этот таск НЕ трогает
  `src/*.ts`.
- Рабочий каталог в момент старта задачи оказался переключён на локальную ветку
  `design/visual-prototype-v01` другой параллельной сессией (см. `.orchestra/BOARD.md`
  "Важный процессный риск"). Чтобы не мешать этой сессии, работа сделана в отдельном
  `git worktree` под `.worktrees/VIS-001` (существующий проектный паттерн — см. остальные
  записи в `.claude/launch.json` для EXP-007/EXP-011/EXP-013/MKT-001).

## Можно менять

- `spikes/arrow-core/viewer/visual-proto/**` (новый, изолированный путь).
- `spikes/arrow-core/tools/serve.mjs` (только добавить ссылку в консольный вывод).

## Не менять

- `spikes/arrow-core/src/**` — комбат-логика.
- Любые encounter/seed данные (`spikes/arrow-core/encounters/**`).
- Существующие debug-viewer'ы (`encounter.js`, `prologue.js`, `cp-prologue.js`).

## Готово, если

- [x] typecheck/test/build проходят без изменений в `src/`.
- [x] визуальный shell открывает seed 10 (`cp-e4.json`, два врага одновременно) и seed 1571
      (`cp-e5.json`, mini-boss) через debug-селектор сцен.
- [x] board рендерится как реальные arrows (не картинка), с glow/highlight/blocked-стилем.
- [x] AssetManifest с graceful placeholder, если PNG нет.
- [x] presentation-only animation hooks не меняют combat timing/state.

## Проверить

```
cd spikes/arrow-core
npm run typecheck
npm test
npm run build
npm run viewer   # или node tools/serve.mjs <port>, затем /viewer/visual-proto/
```
В браузере: выбрать сцену в шапке, тапнуть по стрелке, убедиться что HP/ATTACK IN/Rotate
обновляются как в `cp-prologue.html`, что попадание/промах/атака врага/смерть цели проигрывают
анимацию, а отсутствующие PNG не ломают рендер.

## Итог

RESULT: см. финальный отчёт в сообщении исполнителя пользователю (BRANCH/SHA/архитектура/скриншоты).

VERIFY: typecheck OK, `npm test` 93/93 OK, `npm run build` OK, проверено вручную в браузере
(две сцены, клики по стрелкам, hit/miss/enemy-attack/countdown, debug-панель).

FOUND: ничего вне рамок задачи не найдено и не чинилось.

После этого:
1. STATUS: DONE (см. выше);
2. RESULT_SHA дозаполнен после push;
3. этот файл закоммичен;
4. исполнитель останавливается и ждёт решения пользователя.
