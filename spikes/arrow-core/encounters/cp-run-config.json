{
  "_notes": "EXP-010 provisional run config, not user-approved (docs/COMBAT-RULES.md 12: exact HP numbers are OPEN). Loaded by viewer/cp-prologue.js for the combat-pressure prologue (encounters/cp-e1..cp-e5.json). ITEM-001: runSeed drives the deterministic reward draft; noRewardAfter skips the draft after the first two tutorial stages (provisional).",
  "playerMaxHp": 10,
  "runSeed": 1,
  "noRewardAfter": [
    "prologue-stage-1",
    "prologue-stage-2"
  ]
}